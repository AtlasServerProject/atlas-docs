# v1.26.7

## Hotfix temporário do NPC visual

Foi testado um fallback com `ArmorStand` para substituir o `FakePlayer` invisível do Auth Lobby.

Resultado:

- o NPC voltou a ser visível;
- o clique continuou funcional pela tag `atlas_npc_auth_emerald`;
- porém o visual ficou abaixo do padrão desejado para o Atlas.

## Decisão

Esta versão foi supersedida pela `v1.26.8`, que migrou o NPC visual para o mod Easy NPC.

